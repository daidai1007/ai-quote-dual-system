# Security

请不要在 Issue、提交或日志中公开 PostgreSQL 口令、API 密钥、客户资料、报价单或图纸。

发现安全问题时，请先通过仓库维护者的私有联系方式报告，不要直接创建包含利用细节的公开 Issue。

本仓库只提供示例配置。真实配置应保存为 `.env`、`client_config.json` 或操作系统环境变量，这些文件已被 `.gitignore` 排除。
