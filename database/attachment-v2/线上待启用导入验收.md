# 线上待启用导入验收

用户在网页SQL编辑器人工执行结构准备、STAGED导入和只读验收，并提供结果截图。本记录依据这些截图；助手未直接连接或修改线上数据库。

## 已确认结果

- 导入函数返回 `xlsx-7b6fcb18de6f8969-r4`，用户提供的COMMIT结果为执行成功。
- 验收返回 `PASS: r4 imported and remains STAGED; no active catalog switch`。

| 字段 | 实际结果 | 核对 |
|---|---:|---|
| data_version | xlsx-7b6fcb18de6f8969-r4 | 与交付版本一致 |
| status | STAGED | 待启用 |
| catalog_total | 479 | 原253 + 新226 |
| active_total | 249 | 与导入前活动数量一致 |
| classification_total | 475 | 原249 + 新226 |
| selection_total | 245 | 与导入前历史选择数量一致 |
| new_catalog_rows | 226 | 全量新目录 |
| new_active_rows | 0 | 新目录未启用 |
| rules | 62 | 与导入包一致 |

PASS对应的脚本还检查来源哈希、226条新分类关系、32条固定规则、30条计算规则、264条绑定、9条材质适用关系和8条仅快速报价附件。截图未单独展开15行代表性成本结果，不把它描述成已人工逐行比对。

历史选择的数量已确认未减少；本次截图并非历史记录逐字段一致性的独立审计。迁移及导入脚本不删除历史选择或旧目录，相关保留行为已通过本地测试。

## 后续边界

当前数据库阶段的结构准备、待启用导入和验收已完成，不再重复运行导入脚本。旧活动目录尚未停用，新目录尚未切换；现有报价继续使用旧活动目录。

API、客户端及后续导出尚需接入新规则与快照，当前不代表双附件金额功能已上线。并柜规则仍按用户要求暂不修改。兼容完成并取得另行切换授权后，应对这个已STAGED版本调用switch，不再stage/activate重复导入；操作见《线上人工执行步骤》第5节。
